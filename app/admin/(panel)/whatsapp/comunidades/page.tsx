import WhatsAppCommunitiesClient from "@/app/components/admin/whatsapp/WhatsAppCommunitiesClient";

export const dynamic = "force-dynamic";

/** Comunidades y grupos de WhatsApp: miembros e invitaciones (el chat del grupo sigue en el celular). */
const Page = () => <WhatsAppCommunitiesClient />;

export default Page;
